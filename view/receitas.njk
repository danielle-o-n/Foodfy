{% extends "base.njk" %}
            {% block caixa %}
            <section class="cards">
                {% for item in itens %}
                <div class="card">
                    <img src="{{item.image}}" alt="">
                    <h6>{{item.title}}</h6>
                    <p>por {{item.author}}</p>
                </div>
                {% endfor %}
                {# {% for item in itens %}
                <div class="card">
                    <img src="{{item.image}}" alt="">
                    <h6>{{item.title}}</h6>
                    <p>por {{item.author}}</p>
                </div>
                {% endfor %} #}
                </section>
                <script src="script.js"></script>
                </div>

            {% endblock %}
